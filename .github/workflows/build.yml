name: PrisonMaden Derleme

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Kodu indir
        uses: actions/checkout@v4

      - name: JDK 25 kur
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '25'
          cache: 'maven'

      - name: Maven ile derle
        run: mvn -B clean package

      - name: PrisonMaden.jar dosyasini yukle
        uses: actions/upload-artifact@v4
        with:
          name: PrisonMaden-jar
          path: target/PrisonMaden.jar
